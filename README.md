# t2askdock

VS Codeのタスク管理ツール拡張です。

## 開発起動手順

1. 依存関係をインストール
   ```bash
   npm install
   ```
2. ビルド
   ```bash
   npm run compile
   ```
3. VS Codeでこのフォルダを開き、`F5`（`Run t2askdock Extension`）で拡張機能を起動
4. コマンドパレットで `Task Dock: Open Tree` などの `Task Dock` コマンドを実行して起動確認
